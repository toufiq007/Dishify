/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "plus.unsplash.com"], // Add 'images.unsplash.com' here
  },
};

export default nextConfig;
